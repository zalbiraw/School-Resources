#ifndef SERIAL_H
#define SERIAL_H

#include <stdlib.h>
#include <stdio.h>
#include <time.h>
#include <math.h>

#define MAX_VALUE 256

void sort(int*, int*, int*, int);
void print_array(const char*, int*, int);

#endif
